import java.util.Stack
public class Myclass {
  public static void main(String arg[]) {
    Stack myStack = new Stack();             // Creating a new stack Data Structure called 'myStack'
    myStack.push("hi");                      // Pushed a string phrase called 'Hi" onto the top of the stack
    myStack.pop();                           // Pushed that same string phase from the top of the stack
  }
}
